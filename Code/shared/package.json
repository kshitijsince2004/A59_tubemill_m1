{
  "name": "@a59/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "babel src --out-dir dist --extensions .js --config-file ../babel.build.cjs",
    "test:validation": "npm run build && node dist/validation/selftest.js"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
