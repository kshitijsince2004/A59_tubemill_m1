{
  "name": "a59-tubemill-m1",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "start": "node server/dist/index.js",
    "migrate": "npm run migrate -w server",
    "seed": "npm run seed -w server",
    "setup": "npm install && npm run migrate && npm run seed",
    "smoke:machine-head": "npm run smoke:machine-head -w server",
    "reset-db": "docker compose down -v && docker compose up -d db supertokens && npm run migrate && npm run seed",
    "detype": "node scripts/detype.mjs"
  },
  "dependencies": {
    "@netlify/database": "^2.0.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.26.0",
    "@babel/core": "^7.26.0",
    "@babel/plugin-transform-modules-commonjs": "^7.26.0",
    "@babel/plugin-transform-typescript": "^7.26.0",
    "@babel/preset-typescript": "^7.26.0",
    "concurrently": "^9.1.2"
  }
}
