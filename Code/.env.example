# Migrate/seed need the owner role (tubemill). For RLS-faithful app runs (H-2), prefer:
# DATABASE_URL=postgresql://m1_app:m1_app@localhost:5433/tubemill
DATABASE_URL=postgresql://tubemill:tubemill@localhost:5433/tubemill
PORT=3001
TENANT_ID=00000000-0000-4000-8000-000000000001
COLLECTOR_MODE=sim
BC_ADAPTER=file
# Future OData (unused until BC UAT + custom API pages — OQ-03):
# BC_BASE_URL=
# BC_TENANT=
# BC_CLIENT_ID=
# BC_CLIENT_SECRET=
SERVICE_TOKEN=dev-service-token
YIELD_TOLERANCE_PCT=2
AUTH_MODE=dev
# AUTH_STRICT=false
# AUTH_ALLOW_HEADER_ROLE=true
#
# SuperTokens Core (docker compose service `supertokens` on :3567)
SUPERTOKENS_CONNECTION_URI=http://localhost:3567
# SUPERTOKENS_API_KEY=
API_DOMAIN=http://localhost:3001
WEBSITE_DOMAIN=http://localhost:5173
# Must match client SuperTokens apiBasePath. Vite + Netlify forward /api intact
# (no strip), so SuperTokens recipes live under /api/auth — not /auth.
API_BASE_PATH=/api/auth
WEBSITE_BASE_PATH=/auth
#
# STATIC_APP_ROLE=OPERATOR
# CORS_ORIGIN=*
# BC_PLAN_PATH=
# COLLECTOR_DRIVE=on_demand
#
# Shift handover / auto-boundary (long-running server only)
# AUTO_BOUNDARY_HANDOVER=off|shadow|on
# AUTO_BOUNDARY_HANDOVER_MACHINES=
# SHIFT_STALE_SWEEP_MS=60000
# SHIFT_OVERTIME_GRACE_HOURS=2
# HANDOVER_ALLOW_EARLY=true
#
# Dev: allow claiming a machine when another operator still holds ACTIVE session
# (also implied when AUTH_ALLOW_HEADER_ROLE=true). Production: leave unset/false.
# SESSION_ALLOW_TAKEOVER=true
#
# Netlify: set SUPERTOKENS_CONNECTION_URI to a managed SuperTokens Core URL
# (or self-hosted). Set AUTH_ALLOW_HEADER_ROLE=false and AUTH_STRICT=true.
# Unauthenticated API calls return 401.
# Netlify Database connection is injected via NETLIFY_DATABASE_URL / @netlify/database.
