DATABASE_URL=postgresql://tubemill:tubemill@localhost:5433/tubemill
PORT=3001
TENANT_ID=00000000-0000-4000-8000-000000000001
COLLECTOR_MODE=sim
BC_ADAPTER=file
SERVICE_TOKEN=dev-service-token
YIELD_TOLERANCE_PCT=2
AUTH_MODE=dev
# STATIC_APP_ROLE=OPERATOR
# CORS_ORIGIN=*
# BC_PLAN_PATH=
# COLLECTOR_DRIVE=on_demand
#
# Netlify (demo branch): set SERVICE_TOKEN to a non-default secret.
# AUTH_MODE defaults to static+OPERATOR on Netlify (headers cannot elevate roles).
# Endpoints remain publicly reachable — neither mode authenticates users.
# Netlify Database connection is injected via NETLIFY_DATABASE_URL / @netlify/database.
